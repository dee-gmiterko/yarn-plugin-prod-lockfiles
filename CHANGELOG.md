# Changelog

## 1.0.0

Initial release.

- Generate production-only `yarn.lock` and `package.json` for a focused workspace
- Wrap mode: optionally generate a synthetic wrapper package as the lockfile root
- Configurable workspace package version
